import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  spinner: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  vader: {
    height: 350,
    width: 350,
    top: 300,
  },
});
