import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  spinner: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },
  vader: {
    height: 200,
    width: 200,
  },
});
