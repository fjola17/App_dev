import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    texy: {
        color: "white",
        justifyContent: "space-around",
        padding: 50,
    },
    bg: {
        flex: 1,
        flexDirection: 'row',
    }
});