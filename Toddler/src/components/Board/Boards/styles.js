import { StyleSheet } from 'react-native';
import { boardBackColor } from '../../../styles/colors';

export default StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    backgroundColor: boardBackColor,
  },
});
